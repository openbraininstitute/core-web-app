pre-push:
  parallel: true
  commands:
    eslint:
      glob: '*.{js,jsx,ts,tsx}'
      run: npx eslint --fix 
    prettier:
      glob: '*.{js,jsx,ts,tsx,json}'
      run: npx prettier --write src
# pre-push:
#   parallel: true
#   commands:
#     typescript:
#       run: npm run typecheck
