pre-commit:
  parallel: true
  commands:
    check:
      glob: "*.{js,ts,cjs,mjs,jsx,tsx,json,jsonc}"
      run: pnpx @biomejs/biome check --write {staged_files} --max-diagnostics 100
      stage_fixed: true
