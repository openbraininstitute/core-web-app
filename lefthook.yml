pre-commit:
  commands:
    check:
      priority: 1
      glob: "*.{css,js,ts,cjs,mjs,jsx,tsx,json,jsonc}"
      run: pnpm exec biome check --formatter-enabled=true --write {staged_files}
      stage_fixed: true
