'use client';

export default function VirtualLabProjectAdmin({
  virtualLabId,
  projectId,
}: {
  virtualLabId: string;
  projectId: string;
}) {
  return (
    <div>
      <h1>Project admin page</h1>
    </div>
  );
}
