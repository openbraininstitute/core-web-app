import { OnboardingFlowLayout } from '@/ui/layouts/onboarding-layout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <OnboardingFlowLayout>{children}</OnboardingFlowLayout>;
}
