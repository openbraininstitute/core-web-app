'use client';

/**
 * Placeholder optimization results tab. The bespoke UI (config listing, launch, in/out files)
 * is not built yet — this just renders a stub so the optimize configure editor can mount.
 */
export function OptimizationTab() {
  return (
    <div className="flex h-full w-full items-center justify-center text-neutral-4">
      <span>Optimization results will go here.</span>
    </div>
  );
}
